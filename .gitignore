# Rust
/target
/tests/integration/target/
**/*.rs.bk
# Cargo.lock IS committed: the reproducible-build pipeline (docker/Dockerfile,
# release.yml) pins dependency resolution to it.

# nginx build artifacts (when building against a local nginx source tree)
/.nginx
/nginx-*/
*.so
dist-local/

# Playwright MCP session artifacts (snapshots from browser-driven testing)
.playwright-mcp/
docker/challenge.dev.html
